# Leave this empty for local development. The app automatically uses
# the current browser host on port 5000, so the same build works on
# your computer and on another device connected to the same Wi-Fi.
# For production, set this to your deployed API URL, for example:
# VITE_API_URL=https://api.example.com/api
VITE_API_URL=
